{
  "name": "mocha_test_example",
  "version": "1.0.0",
  "description": "mocha test example for presentation",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ACzero",
  "devDependencies": {
    "mocha": "~3.4.2"
  }
}
